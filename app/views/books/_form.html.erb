<div class="row">
  <div class='col-md-12'>
    <%= simple_form_for(book, wrapper: :horizontal_form, html: { class: 'form-vertical' }) do |f| %>

      <% if book.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>
          <ul>
          <% book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <%= f.input :name %>
      <%= f.input :url %>
      <%= f.submit(class: 'btn btn-success') %>
    <% end %>
  </div>
</div>


<div class='row'>
  <div class='col-md-12'>
    <h3>Chapters</h3>

    <%= render('chapters/index', book: book, chapters: book.chapters) %>
  </div>
</div>
