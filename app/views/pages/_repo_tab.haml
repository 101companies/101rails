%h2 Linked GitHub repo
- if @current_user_can_change_page
  = render "/pages/repo_form"

- if @page.repo_link
  %a.btn.btn-primary{:href => @page.repo_link.full_url, :target => '_blank'}
    %i.icon-github-alt
    Go to GitHub
- else

  %p
    This page doesn't have link to GitHub repo now.
    - if !current_user
      %a{:href => "/auth/github"}
        Click here to log in and continue.

%h2 Matching service findings

- if @page.worker_findings and !@page.worker_findings.empty? and @current_user_can_change_page

  - findings = JSON.parse(@page.worker_findings)
  - if findings
    - findings.each do |one_finding|
      - one_finding.keys.each do |finding_key|
        %p
          %b
            = finding_key.capitalize
          \: #{PageModule.contribution_array_to_string(one_finding[finding_key])}
  - if  (can? :manage, @page)
    - apply_url = @page.url
    %br
    %a.btn.btn-success{:href => "/contribute/apply_findings/#{apply_url}"}
      Insert findings as page metadata

- else
  We don't have any information about used languages, technologies, concepts, and features for this page.
