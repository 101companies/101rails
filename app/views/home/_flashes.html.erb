<!-- growl like notifications (popups) for flash messages -->
<%= js extend_gritter :time => 5000, :fade_out_speed => 500, :fade_in_speed => 'fast' %>
<% flash.each do |key, msg| %>
    <%= js add_gritter(msg, :image => flash_class(key), :title => key.to_s.capitalize) %>
<% end %>
