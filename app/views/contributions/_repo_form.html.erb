<%= react_component('ContributionEditor',
{
   repo_link:   RepoLink.new.as_json(methods: [:namespace, :contrb_repo_link, :page_title, :out_name]),
   repos:       @user_github_repos,
   user_repo:   (@user_github_repos.first if @user_github_repos && @user_github_repos.first)
 }) %>
