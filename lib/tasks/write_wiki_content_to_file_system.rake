task :write_wiki_content_to_file_system => :environment do
  #Page.all.each do |page|
    #File.open("dump_pages/101wiki/#{page.namespace}:#{page.title}", 'w') do |file|
      #begin
        #file.write(page.raw_content)
      #rescue
        #puts "Failed to write file #{page.namespace}:#{page.title}"
      #end
    #end
  #end
end
